# 层积 · 岩芯时序 · 开发日志

> 概念 9：复盘界面里的时间岩芯剖面。信息可视化优先、实用性优先，动效退为受控增强。
> 直接修复概念 8「弦」被否根因（艺术有趣但读不出信息、实用性低）。

## 概念 9 v1（2026-08-16）
- **立项背景**：用户否决概念 8 弦——"很有意思，但在信息可视化上还是很欠缺，无法直观地获得信息，只是作为艺术很有意思，但实用性大大降低了"。并明确要求**每次总结用户反馈**以防创意偏离。
- **护栏动作（先于动手）**：
  - 写 `DesktopApp/docs/用户审美偏好与动效铁律.md`：按概念 0–8 滚动汇总用户全部反馈，提炼最新用户铁律（含新增「信息可视化优先/实用性优先」「每次总结反馈」）。
  - 更新 skill `creative-motion-design`：SKILL.md 加约束⑨（信息可读性/实用性优先），origin.md 加失败案例⑥（弦）+ 约束⑫。
  - 更新跨项目 `~/.workbuddy/MEMORY.md`：加信息可视化优先 + 每次总结要求。
- **设计**：复盘界面，X=时间(90天可滚动)，Y=投入量带刻度轴。每日岩芯柱=专注(琥珀主柱)+正确率(青灰细柱,薄弱日标红)+考点(点簇)+连续打卡(朱砂基线)。强信息：坐标+数值+悬停精确面板+**拖动框选区间实时聚合读数**+图例。动效=悬停抬升/框选高亮/标记今日生长(+42)/滚轮平移，克制零辉光、确定可控、非游戏。
- **技术栈**：Canvas2D 单文件（信息密集场景比 WebGL 更清晰可靠，且零辉光、无黑屏）。
- **文件**：`docs/层积/层积·动效概念原型.html`、`docs/层积/层积·动效概念介绍.md`、本日志。
- **校验**：
  - `node --check` SYNTAX_OK。
  - puppeteer-core + Chrome ANGLE/SwiftShader 1366×860：零错误、RAF 运行、柱被画出；区间聚合正确（全90天 均值专注162/总考点313/最长连续17；末10天 均值专注175/均值正确率36）；标记今日 211→252.68（+42 收敛）；拖动+单击全程零报错。
  - html-review 子集（结构+安全）通过。
- **修复记录**：初版 mark-today 被自动演示遗留选区「吞掉」（单击只清选区未标记）；改为 pointerdown 先 clearSel()，单击稳定标记今日 +42。
- **红线**：仅新增 `docs/层积/` 与主索引/护栏文档；未碰看板娘层/App.cpp/TopBar.cpp/Store.*；未 `git add -A`。
- **待评审**：用户在真实浏览器确认信息是否直观可读、框选聚合是否够用、方向是否对路。

---

方法论（沿用 `creative-motion-design`）：
- 先对齐护栏（用户铁律文档 + skill 约束），再动手——避免重蹈概念 7 森林连否覆辙。
- 信息可视化任务：图表本体（轴/值/标签/可读几何）先于风格层；交互要能直接获取信息（框选聚合）。
- 形态优先于特效；禁用软圆辉光；零辉光、确定可控、真实操作驱动、落真实界面。
